<!DOCTYPE html>
<html lang="en">
<head>
  <%= render partial: 'shared/head' %>
</head>

<body id="<%= "#{controller_name}-#{action_name}" %>" class="mo-<%= module_name %>">
<%= yield %>
</body>
</html>
